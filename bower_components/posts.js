$( document ).ready( function(){

    console.log("aaa");
   // var postTemplate =  Handlebars.compile( $( '#post-template').html());
    var postPreviewTemplate = Handlebars.compile( $( '#post-preview-template' ).html() );
    var postsTemplate = Handlebars.compile( $( '#posts-container__list-template' ).html() );
   // var navigationTemplate = Handlebars.compile( $( '#posts-container__navigation-templat' ).html() );

    

});